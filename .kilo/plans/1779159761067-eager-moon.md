# Plan: Clean Up Leftover Build System Artifacts (Post-CMake→Zig Migration)

## Overview

Remove all CMake and legacy IDE build files now that the project builds exclusively via `build.zig`. Update CI, documentation, and `.gitignore` accordingly.

## Steps

### 1. Remove CMakeLists.txt files (8 files)

| # | Path |
|---|---|
| 1 | `CMakeLists.txt` |
| 2 | `src/CMakeLists.txt` |
| 3 | `src/ui/CMakeLists.txt` |
| 4 | `src/magic/CMakeLists.txt` |
| 5 | `src/interface/CMakeLists.txt` |
| 6 | `src/imgui/CMakeLists.txt` |
| 7 | `src/engine/CMakeLists.txt` |
| 8 | `src/engine/audio/CMakeLists.txt` |

### 2. Remove cmake/ module directory

Delete `cmake/` entirely (33 `.cmake` files in `cmake/Modules/`).

### 3. Remove legacy IDE project directories

- `VS/` — Visual Studio solution + vcxproj files + `.sdf` (IntelliSense database)
- `VS.2015/` — Visual Studio 2015 solution + project files + `.sdf` databases
- `xcode/` — Xcode project files + generated plists/entitlements/assets

### 4. Remove generated Config.hpp duplicates

- `VS-includes/Config.hpp` — was used by the Visual Studio CMake build; canonical source is `src/Config.hpp.in` used by Zig's `addConfigHeader`
- `xcode/Barony/Barony/Config.hpp` — Xcode-specific generated copy

### 5. Update CI scripts to use Zig build

**`.github/workflows/build-linux_fmod_steam.yml`** — Rewrite:
- Remove `cmake` / `make` steps
- Use `zig build` with appropriate options (`-Dfmod -Dsteamworks`)
- Artifact paths change from `build/release/barony` to `zig-out/bin/barony` (or use `--prefix` to customize)

**`.github/workflows/build-linux_fmod_steam_eos.yml`** — Same treatment:
- Two builds (barony + editor) using `zig build` with appropriate flags
- Artifact paths change from `build-barony/release/barony` / `build-editor/release/editor` to `zig-out/bin/`

**`ci/build-linux_fmod_steam.sh`** — Rewrite section 2 (Build from source):
- Keep section 1 (dependency fetching) since it handles Steamworks/FMOD SDK download
- Replace lines 70-87 with `zig build` equivalents:
  - `zig build -Doptimize=ReleaseFast -Dfmod -Dsteamworks -Dgame -Deditor`

**`ci/build-linux_fmod_steam-editor.sh`** — Same treatment:
- Replace cmake/make with `zig build -Doptimize=ReleaseFast -Dfmod -Dsteamworks -Deditor -Dgame=false`

**`ci/build-linux_fmod_steam_eos-barony.sh`** — Same treatment:
- Replace cmake/make with `zig build -Doptimize=ReleaseFast -Dfmod -Dsteamworks -Deos -Dgame -Deditor=false`

**`ci/clean-linux.sh`** — Not cmake-specific, but update:
- Change `rm -rf ../build/` to `rm -rf ../zig-out/` to match new build output directory

### 6. Rewrite INSTALL.md

Replace the entire document with Zig-based build instructions:
- Remove CMake dependency requirement
- Remove Visual Studio / Xcode instructions
- Remove `cmake` command examples
- Document `zig build` usage with `-Ddeps_root`, `-Ddatadir`, `-Doptimize=`, and optional feature flags
- Keep the dependency list (SDL2, etc.) but update acquisition instructions
- Reference AGENTS.md or build.zig for full option details

### 7. Update .gitignore

Remove entries that no longer apply (these tracked files are being deleted):

```
!/VS-includes/Config.hpp       ← remove (VS-includes/ is going away)
```

Keep entries for `zig-out/` and `.zig-cache/` (already present at bottom).

Consider cleaning up the many `VS.2015/` and `xcode/` entries that are now obsolete — but this is optional since they won't match anything after deletion.

### 8. Update AGENTS.md (optional)

Add a note at the end that cleanup was performed, similar to how the existing file documents the migration. This keeps the project state documentation current.

## Execution Order

1. Delete files/directories (steps 1-4) — these are pure removals
2. Edit CI scripts (step 5)
3. Edit documentation (steps 6-8)
4. Verify: `git status` shows expected deletions; no remaining cmake references in active build files

## Verification

After implementation, run:
- `git status` to confirm all intended files are deleted
- `rg -i "cmake" --include="*.{zig,sh,bat,yml}"` to check for any missed references
- `rg -i "cmake" --include="*.md"` to check docs are updated
