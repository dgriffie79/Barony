# Plan: Strip & Simplify Barony for WASM Target

## Goal

Prepare the Barony codebase for eventual WASM deployment (no Emscripten) by:
1. Removing all non-Windows platform code
2. Removing proprietary SDK integrations (Steam, EOS, PlayFab)
3. Removing unnecessary optional dependencies (imgui, TheoraPlayer, cURL, Opus, Tremor)
4. Replacing RapidJSON with a small C JSON library
5. Converting C++ to pure C (keeping audio working throughout)

**Critical constraint: Audio must remain working at every stage.** The game uses FMOD (C++ API). We convert it to FMOD's C API to keep it working during C++→C conversion. Audio is the LAST thing replaced (with Web Audio API for WASM).

**Scope**: ~515K lines, 150 `.cpp` files, 54 `.hpp` files, 3 `.h` files (~465K lines game code, ~48K lines imgui + ~15K lines editor removed)

---

## Audio Preservation Strategy

The audio system stays working through the entire migration:

| Stage | Audio State | How |
|---|---|---|
| Stripped C++ | Working | FMOD C++ API, same as today |
| C++→C conversion | Working | Audio subsystem converted from FMOD C++ API to FMOD C API |
| Pure C | Working | FMOD C API continues to work via `zig cc` |
| C→Zig replacement | Working | C audio code called from Zig via `@cImport` |
| WASM target | Working → replaced | FMOD C API stubbed; Web Audio API backend added |

**FMOD's C API** (`fmod.h`) is a first-class interface, not a wrapper — it provides the same functionality as the C++ API (`fmod.hpp`). The conversion is mechanical:
```cpp
// C++ API (current)
FMOD::System* system;
system->createSound(name, mode, 0, &sound);
system->playSound(sound, 0, false, &channel);

// C API (converted)
FMOD_SYSTEM* system;
FMOD_System_CreateSound(system, name, mode, 0, &sound);
FMOD_System_PlaySound(system, sound, 0, 0, &channel);
```

---

## Phase 1: Config & Build Simplification ✅ DONE

### 1.1 Simplify `Config.hpp.in`

Hardcoded `WINDOWS` only. Kept `@FMOD@` and `@NOT_DWORD_DEFINED@`.

### 1.2 Strip `build.zig`

Removed all `b.option(...)` calls except: `deps_root`, `datadir`, `editor`, `game`, `fmod`. Removed config header substitutions for platform defines and removed features. Removed linking of steam_api, EOSSDK, playfab, jsoncpp, curl, etc. Kept fmod linking.

---

## Phase 2: Strip Platform Code ✅ DONE

### 2.1 Remove non-Windows platform `#ifdef` blocks

~238 instances across game code stripped. Strategy: keep the `#else` / fallback path (Windows/default), remove the `#ifdef` branch.

| Define | Instance Count | Files Affected |
|---|---|---|
| `#ifdef APPLE` | 23 | game.cpp, init.cpp, editor.cpp, savepng.cpp, savepng.hpp, ui_general.cpp |
| `#ifdef LINUX` | 12 | main.cpp, game.cpp, editor.cpp, ui_general.cpp |
| `#ifdef BSD` / `__FreeBSD__` / `__NetBSD__` | 3 | game.cpp |
| `#ifdef PANDORA` | 4 | init.cpp, game.cpp |
| `#ifdef HAIKU` | 1 | game.cpp |
| `#ifdef NINTENDO` | 198 | 22 files (MainMenu.cpp, player.cpp, input.cpp, sound.cpp, etc.) |
| `_MSC_VER` | 3 | init.cpp, scores.cpp, main.hpp |

---

## Phase 3: Strip SDK Integrations ✅ DONE

### 3.1 Remove Steamworks

Deleted `src/steam.hpp` (413), `steam.cpp` (2,431), `steam_shared.cpp` (396).
Created `src/achievements.cpp` with LOCAL_ACHIEVEMENTS fallback path.
Stripped ~110 `#ifdef STEAMWORKS` blocks from remaining files.

### 3.2 Remove EOS

Deleted `src/eos.hpp` (892), `eos.cpp` (~3,300), `eos_editor.cpp` (417).
Stripped ~77 `#ifdef USE_EOS` blocks.

### 3.3 Remove PlayFab

Deleted `src/playfab.hpp` (360), `playfab.cpp` (2,152).
Stripped ~43 `#ifdef USE_PLAYFAB` blocks.

### 3.4 Simplify lobby system

`lobbies.hpp`/`cpp` reduced from 754 → 139 lines. LobbyHandler_t reduced to minimal stub.

---

## Phase 4: Strip OpenAL (Audio Fallback) ✅ DONE

Removed 14 `#ifdef USE_OPENAL` instances across 5 files. Removed ~800-line OpenAL implementation section in sound.cpp. Kept FMOD path.

Also removed `#ifdef USE_TREMOR` (3 instances in sound.cpp).

---

## Phase 5: Strip Optional Dependencies ✅ DONE

### 5.1 Remove imgui
Deleted `src/imgui/` directory (48,493 lines, 7 source files + 8 headers). Stripped 11 `#ifdef USE_IMGUI` blocks.

### 5.2 Remove TheoraPlayer
Stripped 8 `#ifdef USE_THEORA_VIDEO` instances.

### 5.3 Remove cURL
Stripped 4 `#ifdef USE_LIBCURL` instances.

### 5.4 Remove Opus
Stripped 10 `#ifdef USE_OPUS` instances.

---

## Phase 6: Replace RapidJSON with cJSON ✅ DONE

### 6.1 Added cJSON
cJSON.h + cJSON.c (DaveGamble/cJSON, ~4,100 lines) added to `src/`.

### 6.2 Rewrote JSON backing layer
`src/json.cpp` — `JsonFileWriter`/`JsonFileReader` rewritten from RapidJSON to cJSON. `BinaryFileWriter`/`BinaryFileReader` kept as-is (no RapidJSON dependency).

### 6.3 Converted direct RapidJSON usage

| File | Status | Notes |
|---|---|---|
| `light.cpp` | Converted | cJSON_Parse + cJSON_ArrayForEach |
| `book.cpp` | Converted | 5 functions, read/write compiled books |
| `MainMenu.cpp` | Converted | ClassDescriptions, RaceDescriptions, MainMenuBanners |
| `scores.cpp` | Converted | Save/load scores JSON |
| `GameUI.cpp` | Converted | ~39 refs — read-only JSON (HUD settings, etc.) |
| `interface.cpp` | Converted | ~21 refs — follower/callout JSON |
| `init.cpp` | Converted | readTilesJson |
| `init_game.cpp` | Converted | loadAchievementData |
| `interface/identify_and_appraise.cpp` | Converted | readFromFile |
| `interface/consolecommand.cpp` | Converted | Debug JSON export |
| `interface/interface.cpp` | Converted | loadFollowerJSON, loadCalloutJSON |
| `net.cpp` | Converted | P2P state serialization |
| `actmonster.cpp` | Converted | printFollowerTableForSkillsheet |
| `mod_tools.hpp` | Conditional | MOD_TOOLS_CPP guard — rapidjson when compiling mod_tools.cpp, cJSON stubs otherwise |
| `mod_tools.cpp` | Has direct rapidjson includes | 17 mod-support functions stubbed |

### 6.3a Critical issue discovered during conversion: cJSON for-loop increment

During the rapidjson→cJSON conversion of `GameUI.cpp` and `interface.cpp`, a systematic bug was introduced: **all `for`-loop increments were left as `++itr` (pointer arithmetic) instead of being changed to `itr = itr->next` (linked-list traversal).**

**Root cause:** RapidJSON iterators use `operator++()` to advance through an internal array (proper iterator semantics). cJSON stores child items as a **singly-linked list**, so advancing requires `current = current->next`. Using `++itr` on a `cJSON*` just does `itr += sizeof(cJSON)` — jumping to whatever heap data happens to be at that offset.

**Scope:** 37 occurrences across 2 files were fixed:
- `GameUI.cpp`: 24 fixes — `itr`, `val_itr`, `inner_itr`, `ally_itr`, `entry_itr`, `pot_itr`, `itr2`, `options_itr`, `effPos_itr`, `arr`
- `interface/interface.cpp`: 13 fixes — `itr`, `itr2`, `itr3`, `highlightItr`

**Warning for future conversions:** Every `for` loop that iterates `->child` in cJSON MUST use `var = var->next`, never `++var`. This applies to any remaining rapidjson→cJSON conversion work (e.g., the 9 mod_tools modules).

### 6.4 Current RapidJSON dependency state

RapidJSON is NOT transitively exposed through any header. It is a direct dependency of 8 compilation units:

| File(s) | RapidJSON Refs | Reason (remaining) |
|---|---|---|
| 8 mod_tools modules | ~326 total | Compendium, ItemTooltips, game systems — still use rapidjson |

`mod_tools.hpp` has 11 rapidjson references in `#ifdef MOD_TOOLS_CPP` blocks only (compiled only when the mod_tools translation units are being built).

11 other source files use cJSON for their JSON parsing. `GameUI.cpp` and `interface/interface.cpp` are fully migrated.

**Removed:** `mod_tools_irc.cpp` (197 lines, 2 rapidjson refs) — IRC chat handler was unused outside mod_tools.

### 6.5 Refactored monolithic `mod_tools.cpp` → 9 modular files

The original 18,751-line `src/mod_tools.cpp` was split into 9 specialized compilation units + a 72-line stub:

| File | Lines | Contents |
|---|---|---|
| `src/mod_tools.cpp` (stub) | 72 | Global object definitions, static data, includes |
| `src/mod_tools_tooltips.cpp` | 6,060 | `ItemTooltips_t` — item/spell tooltip formatting |
| `src/mod_tools_compendium.cpp` | 6,637 | `Compendium_t` — bestiary, items, magic, codex, world, events |
| `src/mod_tools_gamemode.cpp` | 1,022 | `GameModeManager_t` — tutorial, seeded/challenge runs |
| `src/mod_tools_misc.cpp` | 1,749 | Statue, debug, glyphs, script parser, monster data, shops |
| `src/mod_tools_config.cpp` | 1,270 | Hotbar config, local achievements, gameplay preferences |
| `src/mod_tools_mods.cpp` | 960 | Mod loading/management |
| `src/mod_tools_equip.cpp` | 581 | Equipment model offsets |
| `src/mod_tools_irc.cpp` | 201 | IRC chat handler |
| `src/mod_tools_editor.cpp` | 315 | Editor entity data |

This decomposition makes Phase 8 (C++→C conversion) tractable — each module can be converted independently with clear boundaries.
The old monolithic file is preserved as `src/mod_tools.cpp.bak`.
A shared internal header `src/mod_tools_private.hpp` provides common includes + `MOD_TOOLS_CPP` define.
`build.zig` updated: adds all 9 new files to the source list (editor target was removed in Phase 7).

---

## Phase 7: Remove Editor ✅ DONE

Removed the map editor target entirely from the build and source code.

### 7.1 Deleted editor-only source files

| File | Lines | Description |
|---|---|---|
| `src/editor.cpp` | 10,344 | Map editor main logic |
| `src/entity_editor.cpp` | 610 | Editor-specific entity handling |
| `src/stat_editor.cpp` | 100 | Editor-specific stat handling |
| `src/items_editor.cpp` | 310 | Editor-specific item handling |
| `src/buttons.cpp` | 4,057 | Editor UI button framework |
| `editor.rc` | — | Editor Win32 resource file |

### 7.2 Stripped `#ifdef EDITOR` / `#ifndef EDITOR` preprocessor guards

~448 guard instances across **48 files** surgically removed:

- `#ifdef EDITOR` blocks → deleted (editor-only code)
- `#ifndef EDITOR` blocks → guard removed, code kept (game code)
- `#elif defined(EDITOR)` → branch removed
- Compound conditions (`defined(EDITOR) || defined(NINTENDO)`, `!defined(EDITOR) && !defined(NDEBUG)`) → simplified or removed
- `#if defined(EDITOR)` / `#if !defined(EDITOR)` patterns handled

### 7.3 Simplified `build.zig`

- Removed `editor_sources`, `editor_mod`, `editor_flags`, `-DEDITOR` macro
- Removed `-Deditor` build option, `run-editor` step, `editor.rc`
- Single `game` target with one unified source list
- Now: `zig build -Ddeps_root=... -Ddatadir=...` builds only the game

---

## Phase 8: C++ → C Language Conversion ⏳ PENDING

### 7.1 Containers — Replace std:: library

| C++ Type | Count | C Replacement |
|---|---|---|
| `std::string` | 1,124 | `char*` + length struct |
| `std::vector` | 261 | Dynamic array struct |
| `std::map` | 97 | Sorted array or simple hash table |
| `std::pair` | 79 | Anonymous struct |
| `std::unordered_map` | 18 | Simple hash table or sorted array |
| `std::set` | 17 | Sorted dynamic array |
| `std::list` | 7 | Doubly-linked list struct |
| `std::unordered_set` | 6 | Hash set |
| `std::queue` | 5 | Ring buffer or dynamic array |
| `std::array` | 3 | Plain C array |
| `std::lock_guard` / `std::mutex` | 5 | SDL_mutex etc. |
| `std::max` / `std::min` | 1,892 | `#define MAX(a,b)` / `MIN(a,b)` |

### 7.2 Control flow

| Construct | Count | C Replacement |
|---|---|---|
| `auto` variable init | 6,020 | Explicit type |
| Range-based `for (x : y)` | 605 | Index or pointer loop |
| Lambda expressions | 1,032 | Named function + struct for captures |
| `static_cast<T>(x)` | 936 | `(T)x` |
| `const_cast<T>(x)` | 114 | `(T)x` or fix const-incorrect interfaces |
| `reinterpret_cast<T>(x)` | 36 | `(T)x` |
| `new`/`delete` | 323 | `malloc`/`free` |
| `constexpr` | 104 | `#define` or `static const` |
| Default parameters | 335 | Wrapper functions or varargs |

### 7.3 Types

| Construct | Count | C Replacement |
|---|---|---|
| `enum class` | 13 | Plain `enum` |
| `class` → `struct` | 152 | `struct` (no method access control) |
| Function overloading | Many | Unique function names |
| Constructor/destructor | 31 | `T_init(T*)` / `T_deinit(T*)` |

### 7.4 Convert FMOD C++ API to C API

Files to convert: `engine/audio/sound.hpp`, `defines.cpp`, `init_audio.cpp`, `sound.cpp`, `sound_game.cpp`, `music.cpp`.

**Strategy**: Switch from `#include <fmod.hpp>` to `#include <fmod.h>`. Replace all `->` method calls with `FMOD_*` function calls. Mechanical — every C++ method has a direct C equivalent.

---

## Phase 9: Rename to .c and Update Build ⏳ PENDING

### 8.1 Rename files

Rename all `.cpp` → `.c` after all C++ features removed. Rename `.hpp` → `.h`.

### 8.2 Update build.zig

- Change source file list from `.cpp` to `.c`
- Remove `-std=c++17`, use `-std=c11`
- Remove `link_libcpp = true`
- Remove C++ runtime linking

---

## Dependency Summary

| Dependency | Status | Reason |
|---|---|---|
| SDL2 | KEEP | Windowing, input, timing |
| SDL2_image | KEEP | Image loading |
| SDL2_net | KEEP | UDP networking |
| SDL2_ttf | KEEP | Font rendering |
| PhysFS | KEEP | Search-path overlay for mods, asset resolution |
| libpng | KEEP | PNG save (screenshots) |
| zlib | KEEP | libpng dependency |
| GLEW | KEEP | OpenGL extension loader |
| FMOD | KEEP (C++ API) | Audio — converted to C API in Phase 7p, replaced with Web Audio later |
| cJSON | ADDED | Primary JSON library (11 files use it) |
| RapidJSON | ISOLATED | Only in 3 .cpp files directly, not transitively exposed |

Removed: Steamworks SDK, EOS SDK, PlayFab SDK, OpenAL, TheoraPlayer, Opus, cURL, Tremor, imgui, Editor executable (+editor-only source files), BARONY_SUPER_MULTIPLAYER, NINTENDO (platform define)

---

## Execution Strategy

**Key principle: the game must build and run (with audio) at every stage.**

```
Phases 1-7 ✅ DONE — Builds with zero errors, game loads and runs
Phase 8 (RapidJSON→cJSON for remaining modules) — ⏳ PENDING (deferred)
Phase 9 (C++→C) — ⏳ PENDING
Phase 10 (Rename .cpp→.c) — ⏳ PENDING
```

### Current project state (after Phase 7 + GameUI/interface cJSON conversion):
- **150** `.cpp` / **54** `.hpp` / **3** `.h` files
- **Single executable target** — no editor build
- Build time: ~50s, game loads and runs without crashes
- All platform ifdefs (APPLE, LINUX, BSD, PANDORA, HAIKU, NINTENDO) stripped
- All SDK integrations (Steam, EOS, PlayFab) stripped — including residual guards
- All optional heavyweight deps (imgui, OpenAL, TheoraPlayer, cURL, Opus, Tremor) stripped
- **Editor entirely removed** (~15K lines, 448 guard instances across 48 files)
- **All dead conditional compilation defines removed** (STEAMWORKS, USE_EOS, USE_PLAYFAB, BARONY_SUPER_MULTIPLAYER, NINTENDO, NINTENDO_DEBUG — ~340 instances across 29 files)
- cJSON is the primary JSON library; `GameUI.cpp` and `interface/interface.cpp` fully converted
- RapidJSON isolated to 9 mod_tools_*.cpp modules (~328 refs) — conversion deferred due to complexity
